# AGENTS.md - legacy quarantine

This tree is legacy reference code. It is not the current implementation surface for `fx-hybrid-engine`.

Contributor workflow details still live in the repo root `AGENTS.md` and `docs/DEVELOPER.md`. Folder navigation for this legacy tree lives in `README.md` in this directory.

## Rules

- Do not add new feature work here unless a task explicitly names this path or asks for legacy compatibility work.
- Do not use this tree to infer current commands, package names, artifact names, or broker behavior.
- If you need logic from this tree, port the pattern into `src/fx_hybrid_engine/` and explain the migration in the change summary.
